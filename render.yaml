services:
  - type: web
    name: psychologist-intake-bot
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: RECIPIENT_CHAT_ID
        sync: false
